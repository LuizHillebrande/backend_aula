package com.unifil.cassino.repository;

import com.unifil.cassino.entity.RodadaCrash;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RodadaCrashRepository extends JpaRepository<RodadaCrash, String> {
}
